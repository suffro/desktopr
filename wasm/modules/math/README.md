# Math module

Example Desktopr WASM module exposing `add`, `sub`, `mul`, `div`, `mod`, `pow`,
and `sqrt`. Build it from the repository root with `npm run wasm:build:math`.

Example request:

```json
{"fn":"add","args":[3,5]}
```
