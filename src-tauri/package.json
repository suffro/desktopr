{
    "scripts": {
        "ts:compile": "npx tsc",
        "clean": "rm -rf target && rm -rf gen && cargo clean",
        "build": "npm run ts:compile && cargo tauri build",
        "build:log": "npm run ts:compile && RUST_LOG=tauri_build=trace cargo tauri build",
        "dev:log": "npm run ts:compile && RUST_LOG=tauri_build=trace cargo tauri dev",
        "dev": "npm run ts:compile && cargo tauri dev"
    }
}
