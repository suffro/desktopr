{
    "scripts": {
        "clean": "rm -rf src-tauri/target && rm -rf target && cargo clean",
        "build": "cargo tauri build",
        "build:log": "RUST_LOG=tauri_build=trace cargo tauri build",
        "dev:log": "RUST_LOG=tauri_build=trace cargo tauri dev",
        "dev": "cargo tauri dev"
    }
}