{
  "name": "bubbledesk",
  "version": "0.3.1",
  "description": "Official JS/TS SDK for the Bubbledesk native bridge",
  "main": "dist-sdk/sdk/index.js",
  "types": "dist-sdk/sdk/index.d.ts",
  "module": "dist-sdk/sdk/index.js",
  "sideEffects": false,
  "files": [
    "dist-sdk/**/*",
    "README.md"
  ],
  "scripts": {
    "build": "rm -rf dist-sdk && tsc -p ../tsconfig.sdk.json",
    "pub": "npm publish --access public",
    "prepublishOnly": "npm run build"
  }
}