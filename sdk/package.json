{
  "name": "desktopr",
  "version": "2.2.0",
  "license": "Apache-2.0",
  "description": "Straightforward Node API for the Desktopr native bridge: build and sign lightweight desktop apps using web stacks",
  "main": "dist-sdk/sdk/index.js",
  "types": "dist-sdk/sdk/index.d.ts",
  "module": "dist-sdk/sdk/index.js",
  "sideEffects": false,
  "files": [
    "dist-sdk/**/*",
    "README.md"
  ],
  "scripts": {
    "build": "rm -rf dist-sdk && tsc -p ../tsconfig.sdk.json",
    "pub": "npm run build && npm publish --access public",
    "prepublishOnly": "npm run build"
  }
}
